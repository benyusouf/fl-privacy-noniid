__pycache__/
*.pyc
.venv/
# Leading slash anchors this to the repository root. Without it the pattern
# matches any directory named "data" at any depth — including viz/src/data/,
# whose contents were being silently excluded from the repository.
/data/
results/*/checkpoint.npz
results/**/*.npz
.DS_Store
