torch>=2.2
torchvision>=0.17
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0
opacus>=1.4
medmnist>=3.0              # PathMNIST

# No federated-learning framework dependency (D37, 3 Aug 2026).
# The federated core, the four aggregation strategies and the pairwise-masking
# secure aggregation are implemented in flcore/. Flower was removed: it supplies
# FedAvg and FedProx but neither SCAFFOLD nor MOON, and its Ray-backed simulation
# works against the cross-round client state those two require.
